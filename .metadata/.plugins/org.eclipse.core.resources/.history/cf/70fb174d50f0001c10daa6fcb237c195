package objects;

import javax.swing.ImageIcon;
import javax.swing.JLabel;

public class Disk extends JLabel implements Shape{
	public Disk(ImageIcon img) {
		super(img);
	}

	@Override
	public void setShape(int width, int height) {
		this.setSize(width, height);
	}
}
